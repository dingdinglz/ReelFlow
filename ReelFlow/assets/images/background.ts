/**
 * 背景图片资源
 * 可以替换为实际的项目背景图片
 */

// 使用渐变色作为背景（备用方案）
export const BACKGROUND_GRADIENT = {
  colors: ['#1a1a2e', '#16213e', '#0f3460'],
};

// 如果有实际的背景图片，可以这样导入：
// export default require('./background.png');
